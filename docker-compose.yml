services:
  qwen3-api:
    build: .
    ports:
      - "8001:8000"
    environment:
      - MODEL_PATH=${MODEL_PATH}
      - MODEL_URL=${MODEL_URL}
      - API_KEY=${API_KEY}
    volumes:
      - /opt/models:/models
    restart: unless-stopped
